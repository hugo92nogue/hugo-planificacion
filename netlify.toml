[build]
  command = "npm run build"
  publish = "dist"

# Para que el routing del lado del cliente funcione (recargar /dashboard, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
